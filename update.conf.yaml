- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.local/bin:
    ~/.config/bat:
    ~/.config/karabiner:
    ~/.config/kitty:
    ~/.config/nvim:
    ~/.config/opencode:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.zshenv:
    ~/.zshrc:

- shell:
    - command: ./1.Scripts/setup_homebrew.zsh
      stdout: true
      stdin: true
      stderr: true
