- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.local/bin:
    ~/.config/AstroNvim:
    ~/.config/bat:
    ~/.config/Devstack44nvim:
    ~/.config/karabiner:
    ~/.config/kitty:
    ~/.config/Lazyvim:
    ~/.config/NvChad:
    ~/.config/opencode:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.zshenv:
    ~/.zshrc:

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stdin: true
      stderr: true
