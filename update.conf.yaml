- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.config/karabiner:
    ~/.config/bat:
    ~/.config/Devstack44nvim:
    ~/.config/AstroNvim:
    ~/.config/NvChad:
    ~/.config/Lazyvim:
    ~/.config/kitty:
    ~/.local/bin:

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stdin: true
      stderr: true
